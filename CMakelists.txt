cmake_minimum_required(VERSION 3.10)

add_subdirectory(Components)

if (TARGET_CHIP EQUAL "ATmega328P")
    add_compile_definitions(__AVR_ATmega328P__=1)
else()
    message(FATAL_ERROR "Unsupported target chip: ${TARGET_CHIP}")
endif()